name: Deploy Kiosk Bot

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy bot
        run: |
          cd /home/ec2-user/kiosk-bot
          git pull origin master
          echo "{\"version\": \"1.0.$(git rev-list --count HEAD)\"}" > version.json
          npm install
          sudo systemctl restart kiosk-bot
          sudo systemctl status kiosk-bot --no-pager